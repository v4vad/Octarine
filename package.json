{
  "name": "octarine",
  "version": "1.0.0",
  "description": "Figma plugin for generating color systems with OKLCH support",
  "scripts": {
    "build": "node build.js",
    "watch": "nodemon --watch code.ts --watch ui.tsx --watch 'lib/**/*' --exec 'npm run build'"
  },
  "dependencies": {
    "culori": "^4.0.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-figma-plugin-ds": "^2.3.0"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.98.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  }
}
